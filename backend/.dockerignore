# Keep the build context small: everything here is either regenerated by the
# build or has no business inside a container image.
target/
.mvn/wrapper/maven-wrapper.jar
*.log
.idea/
.vscode/
*.iml

# Never ship secrets into an image layer, even one that is never published.
.env
.env.*
!.env.example
